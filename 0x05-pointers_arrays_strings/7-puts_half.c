#include <stdio.h>

void puts_half(char *str);

/**
 * puts_half -  function that prints half of a string
 *
 * Return: Always 0
 */
int puts_half(void)
{
char *str;
str = "Holberton!";
puts_half(str);
return (0);
}
